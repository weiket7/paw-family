<?php namespace App\Models;

use Eloquent, DB, Validator, Input;

class Report extends Eloquent
{
  public function getSales() {
    //$s = "SELECT sum(nett_total), sum(gross_total), sum(product_discount) from sales "
  }
}